{% extends 'base.html.twig' %}

{% block title %}Modifiezr un Emprunt{% endblock %}

{% block body %}
    <div class="container">
            <div class="container containerform">
                <form class="login-form" method="post"> 

                    <h1>Modifier un Emprunt</h1>

                    {{ include('emprunt/_form.html.twig', {'button_label': 'Update'}) }}

                    <a href="{{ path('app_emprunt_index') }}" class="btn btn-secondary mt-4">Revenir à la list</a>

                    {{ include('emprunt/_delete_form.html.twig') }}
                </div>
            </div>
    </div>   
{% endblock %}
